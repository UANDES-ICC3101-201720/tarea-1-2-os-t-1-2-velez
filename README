Emilio Vélez
(Funciona cuando quiere usando mismos parametros argv, ni idea por que...)


Informe:
Problemas encontrados:
-Falta de flags para compilar el datagen (solved).
-De vez en cuando el programa genera error sin motivo aparente mas que errores de conexion al socket.

Funcionalidad Pendiente:
-Aplicar MUTEX.
